# Equivariant_Flow_Sampling_latex

Kanwar et al., "Equivariant flow-based sampling for lattice gauge theory"
(PRL 125, 121601, 2020; arXiv:2003.06413) 的英文规范化 LaTeX 转排。

编译（两遍）：
```bash
cd build && xelatex -interaction=nonstopmode -halt-on-error ../main.tex
```
成品：`build/main.pdf`
