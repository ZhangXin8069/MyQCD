# AGENTS.md

本目录为 arXiv:2311.18704（Shindler, PRD 110, L051503）的 LaTeX 归一化转排版（article 类）。
编译：`cd build && xelatex -interaction=nonstopmode -halt-on-error ../main.tex`（两遍）；
或 `xelatex -output-directory=build main.tex`。成品 `build/main.pdf`。
