# AGENTS.md

LPC《Unpolarized isovector quark distribution function from Lattice QCD》(arXiv:1807.06566) 的英文 LaTeX 转排目录。

编译：
```bash
cd <dir> && xelatex -interaction=nonstopmode -halt-on-error -output-directory=build main.tex   # ×2
```
成品：`build/main.pdf`。结构：`main.tex` + `chapters/*.tex` + `images/*.pdf`。
