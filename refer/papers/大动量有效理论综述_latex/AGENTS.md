# AGENTS.md

Large-Momentum Effective Theory（Rev. Mod. Phys. 93, 035005 (2021)，arXiv:2004.03543）
的中文全文翻译目录（ctexart，逐节对应英文转排目录 LaMET_RMP_Review_latex）。

编译：
```bash
cd 大动量有效理论综述_latex && xelatex -interaction=nonstopmode -halt-on-error -output-directory=build main.tex   # ×2
```
成品: `build/main.pdf`。结构: `main.tex` + `chapters/*.tex` + `chapters/backmatter.tex` + `images/*.pdf`。
