# 论文原始 PDF 缓存

本目录只保存 `../PyQCD/refer/papers/` 缺失、但论文图谱 P01--P50 必需的 10 份 arXiv 官方 PDF。文件名固定为 `Pxx.pdf`，来源 URL 与 SHA-256 写在上一级 `paper_sources.json`。

缓存文件不得由中文转排本、缺图版本或占位页替换。课程构建器会核对映射、文件存在性、可读页数和已登记哈希；论文内容保持只读，图谱只在外层叠加稳定的 `Pxx-pyyy` 页面 ID。

